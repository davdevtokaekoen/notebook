name: Execute Notebook
on: [push]
jobs:
  run-notebook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jupyter/notebook-action@v1
        with:
          notebook: '*.ipynb'
